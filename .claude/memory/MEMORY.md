# MASCode Project Memory

Index only — one line per memory, detail lives in the topic files. mascode-domain gotchas (CiviCRM / afform / SearchKit / FormProcessor / CiviRules / WPO365). Consult before CiviCRM work. Relocated from the Klaus repo's memory on 2026-07-06 (Klaus keeps cross-project patterns; mascode-domain knowledge lives here).

## Memory Index
- [reference_civicrm_mascode_gotchas.md](reference_civicrm_mascode_gotchas.md) — CiviCRM/mascode gotchas: cv flush after FP import, CiviRule/FP same-transaction ordering, managed-entity update modes (always/unmodified/never), afform :name pseudoconstant refs, afform/SearchKit gotchas (public CSS, PostEvent props, anon-FP UTC, :label, button links, filter traps, download action), SearchKit ACL-as-security (acl_bypass, cv-scr-can't-test-WP-perms, user_contact_id in WHERE), Purifier strips HTML comments, WPO365 VC identity sync + dupe-fix recipe, SES From-identity verification
