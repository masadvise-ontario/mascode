// Angular module that carries the VC case-detail stylesheet
// (css/vc-case-detail.css, declared in mascode.php's hook_civicrm_angularModules).
// CSS-only module — the afsearchMASCaseDetailsVC afform lists "mascodeVcDetail"
// in its .aff.json `requires` so the stylesheet loads on that page only.
(function (angular) {
  'use strict';
  angular.module('mascodeVcDetail', []);
})(angular);
